<?php

declare(strict_types=1);

$helper = Helper::getInstance();

if (!isset($topics) && !is_array($topics) && count($topics) < 1) {
   header('Location: ' . URL_ROOT . '/errorPages/internalError');
   return;
}
?>

<?php if($helper->isLoggedIn()) :?>
   <div class="alert bg-light">
      <a href="<?php echo URL_ROOT . '/topics/create' ?>">
         <i title="Add" class="fas fa-2x fa-plus-square" style="color: lightgreen"></i>
         Add a new question
      </a>      
   </div>
<?php else: ?>
   <i class="fas fa-plus-square"></i>
<?php endif; ?>

<?php
echo '<ul class="list-group">';
// TODO: cursed
$numberOfSuggestions = 3;
// end of cursed code
foreach ($topics as $topic) {
   include APP_ROOT . DIRECTORY_SEPARATOR . 'app' . DIRECTORY_SEPARATOR . 'views' . DIRECTORY_SEPARATOR . 'includes' . DIRECTORY_SEPARATOR . 'topic.phtml';
}
echo '</ul>';

?>