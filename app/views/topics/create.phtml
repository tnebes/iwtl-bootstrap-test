<div class="container mt-5">
   <form method="POST" action="/topics/create">
   <!-- Title input -->
   <div class="form-outline mx-auto mb-4">
      <h1>Title of your question</h1>
      <input type="text" name="name" class="form-control" placeholder="<?php echo Helper::getInstance()->getRandomTopicTitle();?>" value="<?php echo $name?>"/>
      <?php if (!empty($nameError)) : ?>
         <span class="text-warning" role="alert"><?php echo $nameError ?></span>
      <?php endif; ?>
   </div>

   <!-- Description input -->
   <div class="form-outline mb-4">
      <h2>Description of your question</h2>
      <textarea class="form-control" name="description" rows="10" placeholder="Short description goes here."><?php echo $description?></textarea>
      <?php if (!empty($descriptionError)) : ?>
         <span class="text-warning" role="alert"><?php echo $descriptionError ?></span>
      <?php endif; ?>

   </div>

   <!-- Submit button -->
   <div class="container">
      <div class="col-xl">
         <div class="row mx-auto">
            <button type="submit" id="submit" class="btn btn-primary mb-4 mx-auto">Send</button>
            <a href="<?php echo $redirect ?? URL_ROOT;?>" role="button" class="btn btn-light mb-4 mx-auto">Back</a>
         </div>
      </div>   
   </div>   
   </form>   
</div>